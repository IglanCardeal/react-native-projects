import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  touchButton: {
    backgroundColor: '#022',
    padding: 10,
    marginTop: 20,
    borderRadius: 5,
  },
})
